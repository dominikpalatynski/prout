http:
  routers:
    prout:
      rule: "Host(`dashboard.qa.palatynkicloud.com`)"
      entryPoints:
        - websecure
      tls: {}

      service: prout-service

  services:
    prout-service:
      loadBalancer:
        servers:
          - url: "http://host.docker.internal:8090"